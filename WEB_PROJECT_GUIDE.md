# 웹페이지 제작 참고자료 (Claude와 작업할 때)

`회계 증빙 자동서식` 웹앱을 Claude와 3일간 만들면서 겪은 시행착오를 정리한 문서입니다.
다음에 비슷한 웹페이지(특히 1인 실무자용 사내 도구)를 만들 때, 이 문서를 먼저 Claude에게
보여주고 시작하면 이번보다 시행착오를 줄일 수 있습니다.

## 왜 시간이 오래 걸렸나 (3일 프로젝트 회고)

**"성격 탓인가, 지시 방식 문제인가"에 대한 답: 둘 다 아니었던 경우가 더 많습니다.**
짧고 직관적인 지시("정리해줘", "이거 어색해") 자체는 문제가 아니었습니다. 오히려
아래 네 가지가 진짜 원인이었습니다.

1. **디자인/배치는 미리 다 적어도 소용없다** — "헤더가 어색하다", "두 번 눌리는 느낌"
   같은 건 실제로 화면을 눌러봐야만 알 수 있습니다. 지시를 아무리 자세히 써도 건너뛸 수
   없는 단계입니다. → *대응: 말로 설명하는 대신 시안(목업)을 먼저 만들어 보여주고,
   그걸 보고 짧게 피드백하는 방식이 오히려 가장 빠릅니다. (이번 프로젝트에서 실제로 그렇게
   전환한 뒤 속도가 붙었습니다.)*
2. **인쇄/PDF처럼 "실제 환경에서만 드러나는 버그"가 있었다** — 빈 페이지, 스크롤바가
   인쇄되는 문제, 마지막 줄 잘림 같은 버그는 전부 사용자가 실제 컴퓨터·실제 프린터
   (한컴 PDF 등)로 진짜 인쇄해본 뒤에야 발견됐습니다. 샌드박스 환경에서는 재현이 안 되는
   문제였습니다. → *대응: 인쇄·다운로드처럼 "최종 결과물"이 중요한 기능은, 완성됐다고
   선언하기 전에 최소 한 번은 실제 환경에서 사용자가 직접 확인하는 과정을 처음부터
   일정에 넣어두는 게 맞습니다.*
3. **권한 모드가 느렸을 가능성도 있다** — 매 작업(파일 수정·터미널 명령)마다 수동 승인이
   필요한 모드였다면, 이 프로젝트에서만 수백 번 넘게 호출한 만큼 승인 대기 시간이
   누적됐을 수 있습니다. Claude 쪽에서는 "승인 기다림"과 "자동 실행"이 똑같이 보여서
   직접 확인이 안 됩니다. → *대응: 새 프로젝트 시작할 때 인터랙티브 터미널에서
   `/permissions`로 현재 모드를 확인하고, 파일 편집·git 커밋처럼 반복적이고 되돌리기
   쉬운 작업 위주라면 더 자동화된 모드로 바꿔두는 걸 권합니다.*
4. **Claude가 원인을 한 번에 못 맞힌 적도 있었다** — 인쇄 높이 버그는 "A4 배수 반올림
   문제 → 스크롤바 문제 → margin collapse 문제" 순서로 세 번에 걸쳐서야 진짜 원인을
   찾았습니다. 이건 지시 방식과 무관하게 Claude 쪽의 진단 시행착오였습니다.

**요약**: 지시를 더 길게 쓴다고 줄어들 시간이 아니었습니다. 대신 "① 시안부터 보여달라고
요구하기 ② 실제 결과물은 직접 한 번 확인하고 알려주기 ③ 권한 모드를 미리 확인해두기"를
처음부터 규칙으로 정해두면, 같은 결과를 더 적은 왕복으로 얻을 수 있습니다.

## 다음 프로젝트 시작할 때 체크리스트

- [ ] **권한 모드를 먼저 확인한다** — 인터랙티브 터미널에서 `/permissions`로 확인.
      파일 편집·git 커밋처럼 반복적이고 되돌리기 쉬운 작업 위주라면 매번 수동 승인하는
      모드보다 자동화된 모드가 훨씬 빠릅니다.
- [ ] **디자인 토큰을 맨 처음에 한 번 정한다** — 배경/글자/테두리/강조색 5~6개, 버튼
      종류(진하게 채운 것 / 테두리만 / 밑줄 텍스트) 3종 정도. 이후 "이 버튼 색 바꿔줘" 같은
      개별 요청이 줄어듭니다.
- [ ] **인쇄·다운로드·이메일 발송처럼 "밖으로 나가는" 기능은 실제 환경 테스트를 처음부터
      일정에 넣는다.** Claude의 샌드박스에서 통과했다고 끝난 게 아닙니다.
- [ ] **시각적 취향이 걸린 변경(배치, 색, 문구 위치)은 "시안 먼저, 구현은 나중"으로
      요청한다.** 반대로 명백한 버그 수정은 시안 없이 바로 고쳐달라고 하는 게 빠릅니다.
      이 둘을 구분해서 요청하면 왕복이 줄어듭니다.
- [ ] **배포 사이트에 올라가는 링크·리소스는 외부 서비스(로그인 필요한 것) 대신 같은
      저장소 안에 같이 올린다.** (이번에 매뉴얼 링크가 Claude 아티팩트를 가리켜서 로그인
      화면이 뜨는 실수가 있었습니다.)
- [ ] **호스팅 자동배포는 몇 분 지연될 수 있다는 걸 감안한다.** 로컬에서 먼저 충분히
      확인한 뒤, 배포 사이트는 한 번만 스팟 체크하는 게 효율적입니다.

## 이번 프로젝트에서 겪은 기술적 함정 (재사용 가능한 지식)

같은 종류의 기능(브라우저에서 여러 문서를 이어붙여 인쇄, iframe 중첩 등)을 다시 만들 때
아래 항목을 Claude에게 미리 알려주면 같은 시행착오를 반복하지 않습니다.

- **여러 HTML 문서를 하나의 인쇄물로 합칠 때**: html2canvas 같은 "화면 캡처" 라이브러리는
  표 칸선을 제대로 못 그리고 화면 전용 요소까지 찍어버립니다. 대신 숨겨진 iframe 여러 개를
  중첩해 넣고 브라우저의 진짜 `window.print()`를 한 번만 호출하는 방식이 훨씬 안정적입니다.
- **iframe 높이를 재서 인쇄 페이지 경계를 맞출 때**: `.page` 같은 특정 요소의
  `scrollHeight`만 재면 안 됩니다. 그 요소의 첫 자식에 위쪽 마진이 있으면 CSS
  margin-collapse 때문에 실제보다 낮게 측정됩니다. `documentElement.scrollHeight`
  (또는 요소의 실제 `getBoundingClientRect().bottom`) 기준으로 재야 정확합니다.
- **페이지 수를 "A4 배수로 올림" 하지 않는다**: 페이지 구분은 `page-break-after:always`
  CSS 하나로 충분합니다. 높이를 인위적으로 A4 배수로 올리면 필요 없는 빈 페이지가 생깁니다.
- **iframe에 스크롤바가 생기면 인쇄물에도 그대로 찍히는 프린터가 있습니다**
  (한컴 PDF 가상 프린터 등). `scrolling="no"` + `overflow:hidden`으로 항상 막아야 합니다.
- **화면 전용 요소(`@media print`로 숨기는 툴바 등)**는, 중첩된 iframe 안에서는 프린터에
  따라 이 규칙이 무시될 수 있습니다. 인쇄 전에 `@media print` 규칙을 미디어 조건 없이
  강제로 한 번 더 적용해두면 화면과 인쇄 결과가 항상 일치합니다.
- **이미지가 비동기로 로딩되는 서식의 높이를 잴 때**: 고정된 시간(예: 300ms)만 기다리지
  말고, 높이가 더 이상 안 바뀔 때까지(연속 2번 이상 같은 값) 폴링한 뒤 확정하는 게 안전
  합니다.
- **iframe.srcdoc을 같은 값으로 다시 대입하면 브라우저가 리로드를 생략**합니다. 내용을
  새로고침하려면 매번 새 iframe 엘리먼트를 만들어 기존 것과 교체해야 합니다.
- **PDF 결과물 검증**: `pdftoppm`이 없는 환경에서는 `pip install pymupdf`로 PyMuPDF를
  설치해 각 페이지를 PNG로 렌더링해서 직접 눈으로 확인하는 방법이 빠르고 확실합니다.
- **PowerShell로 git commit할 때**: 커밋 메시지에 리터럴 큰따옴표(`"`)가 섞이면
  `-m @'...'@` 히어스트링을 써도 pathspec 오류가 날 수 있습니다. 큰따옴표를 아예 빼고
  쓰는 게 안전합니다.

## 이번에 정리한 디자인 토큰 (참고용)

- 배경 `#f4f5f7` / 카드 `#fff` / 테두리 `#e3e4e8`
- 본문 글자 `#1e2024` / 보조 글자 `#6b6f76`
- 강조색(파랑) `#2f6fed`, 연한 배경 `#eaf1ff` — 실제 배포 화면의 버튼·포커스 강조는
  기존부터 쓰던 `#3b82f6` 계열과 섞여 있어 완전히 통일하진 못했습니다. 다음 프로젝트는
  시작할 때부터 강조색 하나만 정해두는 걸 권합니다.
- 버튼 3종: 진하게 채운 버튼(핵심 동작 1개) / 테두리만 있는 버튼(보조 동작) / 밑줄 텍스트
  (가벼운 이동)
